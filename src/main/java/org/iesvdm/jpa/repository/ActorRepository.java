package org.iesvdm.jpa.repository;

import org.iesvdm.jpa.domain.Actor;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ActorRepository extends JpaRepository<Actor,Long> {
}
